name: project
channels:
  - defaults
dependencies:

  - yaml=0.2.5=h7b6447c_0

  - pip:
      - kazoo==2.10.0
      - pika==1.3.2
      - pip==24.2
      - rabbitmq==0.2.0
prefix: /home/saurabh/anaconda3
