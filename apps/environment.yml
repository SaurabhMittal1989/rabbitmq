name: apps_environment
channels:
  - defaults
dependencies:
  - python=3.11  # Use the installed Python version
  - yaml=0.2.5
  - pip
  - pip:
      - kazoo==2.10.0
      - pika==1.3.2
      - rabbitmq==0.2.0
      - pip==24.2
      - uhashring